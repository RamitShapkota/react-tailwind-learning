# 01 - React Basics 🚀

## Today's Learning
I successfully set up my first React environment and explored the fundamental concepts that make React powerful.

### 📝 Topics Covered
- **The "Why" of React**: Understanding why we use a library instead of Vanilla JS.
- **Vite Setup**: Learned to scaffold a project using `npm create vite@latest`.
- **Project Structure**: Explored the purpose of `node_modules`, `public`, and `src` folders.
- **npm commands**: 
  - `npm install`: To download dependencies.
  - `npm run dev`: To start the local development server.

### ⚙️ How to Run
1. Navigate to this folder: `cd 01-react-basics`
2. Install dependencies: `npm install`
3. Run the project: `npm run dev`

---
*Learning inspired by the MERN Stack path.*