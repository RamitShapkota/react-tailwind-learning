# React Props & Tailwind CSS Practice

A simple project to demonstrate how to pass data via **Props** and style components using **Tailwind CSS**.

## 🚀 Key Features
- **Props Object:** Passing dynamic data (strings, colors) from Parent to Child.
- **Tailwind CSS:** Utility-first styling for rapid UI development.
- **Reusable Components:** A single `Card` component used multiple times with different data.

## 🛠️ Installation & Setup

1. **Install Tailwind CSS:**
   ```bash
   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p

2.**Configure paths:** Add your files to tailwind.config.js.

3.**Add directives:** Add  @tailwind to your index.css.

